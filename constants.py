# Constants
# (Pixel) Size of each cell in the grid
CELL_SIZE = 60
# Number of cells in the grid
GRID_SIZE = 8
# (Pixel) Width of the grid
GRID_WIDTH = GRID_SIZE * CELL_SIZE
# (Pixel) Height of the grid
GRID_HEIGHT = GRID_SIZE * CELL_SIZE
# (Pixel) Width of the window
WINDOW_WIDTH = GRID_WIDTH + 400  # Extra space for blocks on the right
# (Pixel) Height of the window
WINDOW_HEIGHT = GRID_HEIGHT + 200  # Extra space for UI elements
# (Pixel) Offset of the grid from the left side of the window
GRID_OFFSET_X = 50
# (Pixel) Offset of the grid from the top side of the window
GRID_OFFSET_Y = 100

# Colors
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
GRAY = (200, 200, 200)
DARK_GRAY = (100, 100, 100)